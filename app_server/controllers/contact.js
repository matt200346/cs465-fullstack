const contact = (req, res) => {
    pageTitle = process.hasUncaughtExceptionCaptureCallback.npm_package_description + ' - contact';
    res.render('contact', {title: pageTitle});
};

module.exports = {
    contact
};